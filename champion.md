```mermaid
 graph TD
    subgraph "Клиент"
        %% Определяем узлы для группы 'Клиент'
        A["1. Пишет в WhatsApp: Хочу аккаунт/играть"]
        F["4. Переходит по ссылке ОПЛАТЫ (полученной от оператора)"]
        G["5. Вносит платеж (Betatransfer)"]
        I["Может войти в ЛК используя Код Доступа и Ссылку на проект"]
    end
    subgraph "Оператор / Админка"
         %% Определяем узлы для группы 'Оператор / Админка'
        B["2. Получает запрос"]
        C["3a. Создает аккаунт клиента в Админке"]
        D["3b. Получает/Генерирует Код Доступа (Данные для входа)"]
        E["3c. Отправляет Клиенту разом:<br/>Код Доступа<br/>Ссылку на проект<br/>Ссылку на оплату (привязанную к ЛК)"] %% Используем <br/> для переноса строк
        K["7. Видит статус клиента/оплату в Админке"]
    end
    subgraph "Система / Betatransfer"
         %% Определяем узлы для группы 'Система / Betatransfer'
         H["6. Платеж успешен => Аккаунт (ЛК) клиента автоматически пополнен"]
    end

    %% Определяем связи между узлами
    A --> B;
    B --> C;
    C --> D;
    D --> E;
    E --> F;
    F --> G;
    G --> H;
    H --> K;
    E --> I;
```
